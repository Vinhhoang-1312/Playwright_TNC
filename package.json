{
  "name": "playwright-project",
  "version": "1.0.0",
  "description": "Playwright automation project",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:parallel": "playwright test --workers=4",
    "allure:report": "allure generate reports/allure-results --clean -o reports/allure-report && allure open reports/allure-report",
  "clean:artifacts": "node ./scripts/cleanArtifacts.js",
    "archive:allure": "node ./ci/archiveAllure.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/xlsx": "^0.0.35",
    "allure-playwright": "^2.0.0",
    "archiver": "^5.3.1",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
